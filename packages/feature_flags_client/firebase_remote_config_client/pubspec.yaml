name: firebase_remote_config_client
description: A Firebase Remote Config-backed feature flags client.
version: 1.0.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  feature_flags_client:
    path: ../feature_flags_client
  firebase_remote_config: ^5.4.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.2.0
