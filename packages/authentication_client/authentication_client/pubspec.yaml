name: authentication_client
description: Authentication client with an HTTP backend implementation.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  equatable: ^2.0.8
  http: ^1.2.2
  meta: ^1.18.0
  rxdart: ^0.28.0
  token_storage:
    path: ../token_storage

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.30.0
  very_good_analysis: ^10.2.0
