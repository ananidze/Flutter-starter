# Project-specific R8/ProGuard rules.
#
# Keep this file in place even when it is empty so release builds can enable
# minification without referencing a missing rules file. Add SDK-specific keep
# rules here when integrating providers that require them.
